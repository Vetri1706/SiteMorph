import { Forma } from "forma-embedded-view-sdk/auto";

// Kept in a separate module so standalone mock previews never evaluate the
// embedded-host SDK. Live Forma runs load this module on demand.
export { Forma };
